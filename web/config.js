// Set your backend API base URL here (no trailing slash).
// Example:
// const API_BASE_URL = 'https://my-backend.onrender.com';

const API_BASE_URL = '';

/*
 After editing, commit and push. The frontend will use this value to call the backend.
*/
// Copy this file to config.js and set API_BASE_URL to your backend URL (no trailing slash)
// Example: const API_BASE_URL = 'https://my-backend.onrender.com';
const API_BASE_URL = '';

/*
If left empty, the frontend will use relative URLs (same origin). For Vercel deployment,
set this to your backend service URL so the static site calls the API correctly.
*/
